#include<stdio.h>
#include<math.h>


int main(){
    double det(double x, double y){
        return 2*x*(12*pow(y,2) - 1);
    }

    double f1(double x, double y){
        return 4*pow(x, 2) + pow(y,2) - 5;
    }
    double f2(double x, double y){
        return pow(x, 2) + pow(y, 3) - 4;
    }

    double eq



    double x0 = 1;
    double y0 = 0;
    int n = 3;
    for(int i=0; i<n; i++){
        double x1 = eq
    }

    return 1;
}